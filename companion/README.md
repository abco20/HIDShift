# HIDShift Companion

The Companion reuses the Web manager UI and connects through native USB Serial
when available, with Bluetooth as fallback. Build the package for the current
OS with:

```sh
mise run companion:build
```
