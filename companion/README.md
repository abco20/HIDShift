# HIDShift Companion

The Companion adds native device discovery, background reconnection, switch
notifications, and tray controls to the manager UI. It prefers the dedicated
USB HID management interface and falls back to Bluetooth automatically. Build the package for
the current OS with:

```sh
mise run companion:build
```
