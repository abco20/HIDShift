[package]
name = "hidshift-client"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false

[features]
debug-serial = []

[dependencies]
hidshift = { path = "../..", features = ["dual-s3-wired"] }
